# Notebooks

This directory contains exploratory notebooks for dataset inspection, tactile
trace visualization, model debugging, and benchmark analysis.

Start with `01_dataset_exploration.ipynb`. It is intentionally lightweight and
uses the synthetic JSON dataset generated by:

```bash
python scripts/generate_synthetic_dataset.py --episodes 16 --steps 64
```
