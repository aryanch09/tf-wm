# Processed Data

Processed data is derived from `data/raw` and can be regenerated. Typical
contents include:

- fixed-length training windows;
- feature caches for tactile or vision encoders;
- normalized tensor shards;
- benchmark-ready evaluation subsets.

Keep preprocessing commands in experiment notes or scripts so results are
reproducible.
